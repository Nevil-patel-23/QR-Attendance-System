package com.university.attendance.models;

/**
 * The three user roles in the system.
 * Stored in the 'role' column of the 'users' table.
 */
public enum Role {
    ADMIN,
    TEACHER,
    STUDENT
}
